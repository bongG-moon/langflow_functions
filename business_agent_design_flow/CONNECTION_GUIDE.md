# 업무 AI 에이전트 설계 Flow 연결 가이드

이 flow는 업무 설명을 입력받아 AI 에이전트 설계서를 만들어주는 체험용 Langflow flow입니다.

처음에는 LLM 없이 기본 결과를 확인하고, 이후 프롬프트 템플릿과 LLM을 연결하는 방식으로 확장하는 것을 권장합니다.

## 1. 컴포넌트 등록

Langflow 커스텀 컴포넌트 경로에 아래 폴더를 추가합니다.

```text
C:\Users\qkekt\Desktop\기능flow\business_agent_design_flow\langflow_components\business_agent_design_flow
```

Langflow에서 컴포넌트가 보이지 않으면 서버 또는 화면을 새로고침합니다.

## 2. 가장 빠른 확인 흐름

LLM 없이 기본 설계 결과를 보고 싶으면 아래처럼 연결합니다.

| 순서 | From | Output | To | Input |
| --- | --- | --- | --- | --- |
| 1 | 00 업무 설명 입력 | 업무 요청 | 01 업무 프로세스 구조화 | 업무 요청 |
| 2 | 01 업무 프로세스 구조화 | 업무 구조화 결과 | 02 AI 에이전트 기능 카탈로그 | 업무 구조화 결과 |
| 3 | 02 AI 에이전트 기능 카탈로그 | 기능 카탈로그 결과 | 04 AI 에이전트 설계 결과 정리 | 기능 카탈로그 결과 |
| 4 | 04 AI 에이전트 설계 결과 정리 | AI 에이전트 설계 결과 | 05 사용자용 설계서 출력 | AI 에이전트 설계 결과 |
| 5 | 05 사용자용 설계서 출력 | 사용자용 설계서 | Chat Output | input |

이 방식에서는 `04 AI 에이전트 설계 결과 정리`의 `LLM 설계 응답` 입력을 비워둡니다.

## 3. LLM 연결 흐름

LLM이 업무를 더 풍부하게 해석하고 개선 아이디어를 만들게 하려면 아래처럼 연결합니다.

| 순서 | From | Output | To | Input |
| --- | --- | --- | --- | --- |
| 1 | 00 업무 설명 입력 | 업무 요청 | 01 업무 프로세스 구조화 | 업무 요청 |
| 2 | 01 업무 프로세스 구조화 | 업무 구조화 결과 | 02 AI 에이전트 기능 카탈로그 | 업무 구조화 결과 |
| 3 | 02 AI 에이전트 기능 카탈로그 | 기능 카탈로그 결과 | 03 AI 에이전트 설계 프롬프트 준비 | 기능 카탈로그 결과 |
| 4 | 03 AI 에이전트 설계 프롬프트 준비 | 업무_요청_JSON | 프롬프트 템플릿 | 업무_요청_JSON |
| 5 | 03 AI 에이전트 설계 프롬프트 준비 | 업무_프로세스_컨텍스트_JSON | 프롬프트 템플릿 | 업무_프로세스_컨텍스트_JSON |
| 6 | 03 AI 에이전트 설계 프롬프트 준비 | 에이전트_기능_카탈로그_JSON | 프롬프트 템플릿 | 에이전트_기능_카탈로그_JSON |
| 7 | 03 AI 에이전트 설계 프롬프트 준비 | 추가_지시사항 | 프롬프트 템플릿 | 추가_지시사항 |
| 8 | 03 AI 에이전트 설계 프롬프트 준비 | 작성_규칙 | 프롬프트 템플릿 | 작성_규칙 |
| 9 | 03 AI 에이전트 설계 프롬프트 준비 | 출력_스키마_JSON | 프롬프트 템플릿 | 출력_스키마_JSON |
| 10 | 프롬프트 템플릿 | 프롬프트 | LLM | input |
| 11 | 02 AI 에이전트 기능 카탈로그 | 기능 카탈로그 결과 | 04 AI 에이전트 설계 결과 정리 | 기능 카탈로그 결과 |
| 12 | LLM | text 또는 message | 04 AI 에이전트 설계 결과 정리 | LLM 설계 응답 |
| 13 | 04 AI 에이전트 설계 결과 정리 | AI 에이전트 설계 결과 | 05 사용자용 설계서 출력 | AI 에이전트 설계 결과 |
| 14 | 05 사용자용 설계서 출력 | 사용자용 설계서 | Chat Output | input |

프롬프트 템플릿에 넣을 본문은 아래 파일을 사용합니다.

```text
business_agent_design_flow/docs/PROMPT_TEMPLATE.md
```

## 4. 00 업무 설명 입력에 넣는 값

### 업무 설명

필수 입력입니다. 사람이 실제로 하는 업무를 자연어로 적습니다.

예시:

```text
매일 아침 설비별 생산량과 불량률을 엑셀로 내려받아 전일 대비 급증한 설비를 확인합니다.
이상 설비가 있으면 원인 후보를 정리해서 담당자에게 메일로 공유하고, 긴급 건은 팀장 승인 후 후속 조치를 등록합니다.
```

### 업무 목적/대상

선택 입력입니다. 결과를 누가 보고 무엇을 결정하는지 적습니다.

### 사용 데이터/시스템

선택 입력입니다. Excel, DB, API, 사내 시스템, 메일, 문서 등 사용하는 데이터를 적습니다.

### 제약사항

선택 입력입니다. 보안, 승인, 사람이 꼭 봐야 하는 단계, 자동화하면 안 되는 작업을 적습니다.

### 원하는 결과물

선택 입력입니다. 리포트, 알림, 메일 초안, 처리 체크리스트, 대시보드 등 원하는 산출물을 적습니다.

## 5. 03 추가 설계 지시사항에 넣으면 좋은 내용

LLM 연결 흐름에서만 사용됩니다.

예시:

```text
초보 Langflow 개발자가 따라 만들 수 있게 노드 수를 너무 늘리지 말고 1차/2차 구현으로 나눠줘.
사내 시스템 업데이트는 실제 자동 실행하지 말고 승인 후 실행 후보로만 제안해줘.
기존 기능flow 중 데이터 조회가 필요하면 reusable_data_flow를, 결과 리포트가 필요하면 html_report_flow를 추천해줘.
```

## 6. 추가 기능을 자연어로 입력하는 흐름

기본 기능 외에 사내에서 사용할 수 있는 기능이나 이미 만든 flow가 있으면, 사람이 자연어로 설명하고 LLM이 기능 카탈로그 JSON으로 바꾸게 할 수 있습니다.

이 흐름은 `02 AI 에이전트 기능 카탈로그` 앞에 선택적으로 붙입니다.

| 순서 | From | Output | To | Input |
| --- | --- | --- | --- | --- |
| 1 | 01 업무 프로세스 구조화 | 업무 구조화 결과 | 02-1 추가 기능 JSON 프롬프트 준비 | 업무 구조화 결과 |
| 2 | 02-1 추가 기능 JSON 프롬프트 준비 | 추가_기능_자연어 | 프롬프트 템플릿 | 추가_기능_자연어 |
| 3 | 02-1 추가 기능 JSON 프롬프트 준비 | 업무_컨텍스트_JSON | 프롬프트 템플릿 | 업무_컨텍스트_JSON |
| 4 | 02-1 추가 기능 JSON 프롬프트 준비 | 기능_JSON_작성_규칙 | 프롬프트 템플릿 | 기능_JSON_작성_규칙 |
| 5 | 02-1 추가 기능 JSON 프롬프트 준비 | 기능_JSON_스키마 | 프롬프트 템플릿 | 기능_JSON_스키마 |
| 6 | 프롬프트 템플릿 | 프롬프트 | LLM | input |
| 7 | LLM | text 또는 message | 02-2 추가 기능 JSON 정리 | LLM JSON 변환 응답 |
| 8 | 02-2 추가 기능 JSON 정리 | 추가 기능 카탈로그 JSON | 02 AI 에이전트 기능 카탈로그 | 추가 기능 카탈로그 JSON |
| 9 | 01 업무 프로세스 구조화 | 업무 구조화 결과 | 02 AI 에이전트 기능 카탈로그 | 업무 구조화 결과 |

프롬프트 템플릿에 넣을 본문은 아래 파일을 사용합니다.

```text
business_agent_design_flow/docs/FEATURE_CATALOG_PROMPT_TEMPLATE.md
```

`02-1 추가 기능 JSON 프롬프트 준비`의 `추가 기능 자연어 설명`에는 아래처럼 자연스럽게 적으면 됩니다.

```text
우리 팀에는 사내 티켓 조회 API가 있습니다.
티켓 ID를 넣으면 티켓 상태, 담당자, 최근 처리 이력을 조회할 수 있습니다.
처음에는 조회 전용으로만 쓰고, 티켓 수정이나 등록은 사람 승인 뒤에만 하도록 설계해주세요.

또 결과를 담당자에게 보낼 메일 초안 생성 기능도 있으면 좋겠습니다.
자동 발송은 하지 말고 제목과 본문 초안까지만 만들어야 합니다.
```

`02-2 추가 기능 JSON 정리`는 LLM 응답을 검증해서 아래 형태의 JSON 문자열로 바꿉니다.

```json
{
  "catalog_notes": ["사용자 자연어 설명을 기반으로 추가한 기능입니다."],
  "capabilities": [
    {
      "capability_id": "user_added_xxxxx",
      "display_name": "사내 티켓 조회",
      "category": "integration",
      "beginner_use_case": "티켓 상태와 담당자를 조회합니다.",
      "when_to_use": "업무가 티켓 상태 확인이나 처리 이력 조회를 포함할 때",
      "needed_inputs": ["티켓 ID"],
      "typical_outputs": ["티켓 상태", "담당자", "최근 처리 이력"],
      "difficulty": "중급",
      "implementation_hint": "처음에는 조회 전용으로 연결하고 등록/수정은 사람 승인 뒤 진행합니다.",
      "source_reference": "user_input:natural_language"
    }
  ]
}
```

JSON에 익숙한 사용자는 `02 AI 에이전트 기능 카탈로그`의 `추가 기능 카탈로그 JSON`에 위 구조를 직접 붙여넣어도 됩니다.

## 7. 출력 결과

`05 사용자용 설계서 출력`은 아래 내용을 Markdown으로 보여줍니다.

- 한눈에 보는 요약
- 핵심 카드
- 업무 프로세스 로직 표
- 사람이 하는 일과 AI 에이전트가 도울 일
- AI 에이전트화 개선 아이디어
- Langflow 구현 흐름
- 기존 기능flow 재사용 후보
- 초보자용 구현 순서
- 추가로 있으면 좋은 정보
- 참조 정보와 출처 링크
- 최종 JSON

## 8. 기존 기능flow와 연결하는 기준

이 flow는 실제 자동화를 바로 실행하지 않고, 어떤 flow를 붙이면 좋은지 설계합니다.

- 데이터 조회가 핵심이면 `reusable_data_flow`를 추천합니다.
- 조회 결과를 리포트로 보여줘야 하면 `html_report_flow`를 추천합니다.
- 외부 시스템을 여러 개 조작해야 하면 `에이전트와 도구` 또는 `MCP 도구`를 2차 확장안으로 제안합니다.
- 승인, 고객 발송, 시스템 업데이트는 `human_review_gate` 패턴을 우선 제안합니다.
